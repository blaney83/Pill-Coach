<div class="col-10 col-md-3 col-lg-3 d-flex justify-content-center">
    <button id={{idString}} class="col tile" deleteTarget={{id}}>
        <i class="deleteButton" id={{idString}}>X</i>
        <div class="overlay"></div>
        <div class="pillName" id={{idString}}>{{rx_name}}</div>
    </button>
</div>